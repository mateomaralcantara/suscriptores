# Pruebas

```powershell
npm test
npm run typecheck
npm run build
```

Las pruebas incluidas validan precios, cantidades, estados, idempotencia, balances, comisiones, límites y datos demo. Las pruebas E2E reales deben añadirse al conectar autenticación y base de datos persistente.
