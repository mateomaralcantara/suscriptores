import Link from "next/link";
import { PageHeader } from "@/components/PageHeader";
import { StatusBadge } from "@/components/StatusBadge";
import { tickets } from "@/lib/demo-data";
export default async function TicketDetailPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const ticket = tickets.find((item) => item.id === id) ?? tickets[0]; return <><PageHeader title={ticket.id} description={ticket.subject} action={<Link className="btn" href="/tickets">← Tickets</Link>} /><div className="grid two"><section className="card"><h3>Conversación</h3><div className="notice">Cliente: Necesito revisar esta operación dentro del entorno sandbox.</div><div className="notice" style={{ marginTop: 12 }}>Soporte: Recibimos la solicitud y verificamos los eventos simulados.</div><div className="field" style={{ marginTop: 16 }}><label>Responder</label><textarea placeholder="Escribe una respuesta" /></div><button className="btn primary" style={{ marginTop: 12 }} type="button">Enviar respuesta demo</button></section><section className="card"><h3>Detalles</h3><div className="list"><div className="list-item"><span>Estado</span><StatusBadge status={ticket.status} /></div><div className="list-item"><span>Categoría</span><strong>{ticket.category}</strong></div><div className="list-item"><span>Prioridad</span><strong>{ticket.priority}</strong></div><div className="list-item"><span>Agente</span><strong>Soporte Demo</strong></div><div className="list-item"><span>SLA</span><strong>4 horas</strong></div></div></section></div></>; }
